import React from "react";
import "./Menu.css";

class Menu extends React.Component {
  render() {
    return (
      <div className="Menu">
        <button>Add</button>
        <button>Login</button>
      </div>
    );
  }
}

export default Menu;
